# SSE transport configuration
# Host address to bind to (default: 0.0.0.0 - all interfaces)
SSE_HOST=0.0.0.0
# Port for the SSE server to listen on
SSE_PORT=<YOUR_SSE_PORT>
# Host port mapping (in case you want to map to a different port on the host)
# Only change this if you need a different external port
HOST_PORT=<YOUR_HOST_PORT>
# Puppeteer Configuration
# Set to true to indicate running in a Docker container (enables proper headless mode)
DOCKER_CONTAINER=true
# Puppeteer launch options (JSON string)
# Example: {"headless": true, "defaultViewport": {"width": 1280, "height": 720}}
PUPPETEER_LAUNCH_OPTIONS="{\"headless\": true}"
# Allow dangerous Puppeteer launch options that might reduce security
# Set to "true" to allow options like --no-sandbox
ALLOW_DANGEROUS=false